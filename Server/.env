MAIL_FROM="AutoTrade <no-reply@autotrade.local>"

# SMTP (choose one config you actually use)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=youraddress@gmail.com
SMTP_PASS=your_gmail_app_password   # use an App Password, not your login

# Fallback recipient if user hasn’t set email in the UI
FALLBACK_EMAIL=yourname@example.com

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=savaniyakishan67@gmail.com
SMTP_PASS=123456789123456
MAIL_FROM="AutoTrade <youraddress@gmail.com>"
FALLBACK_EMAIL=youraddress@gmail.com

# --- EOD profit guard ---
MUST_END_DAY_PROFITABLE=1     # if 1, don't square-off while net < 0
EOD_HARD_CUTOFF_IST=15:25     # final forced square-off time (IST, HH:MM)


# Which broker a user chooses will be per-user; these are app keys.
# Zerodha
ZERODHA_API_KEY=xxxxx
ZERODHA_API_SECRET=yyyyy
ZERODHA_REDIRECT_URL=http://localhost:4000/api/broker/callback/zerodha

# Upstox
UPSTOX_API_KEY=xxxxx
UPSTOX_API_SECRET=yyyyy
UPSTOX_REDIRECT_URL=http://localhost:4000/api/broker/callback/upstox

# Angel One (SmartAPI)
ANGEL_API_KEY=xxxxx
ANGEL_API_SECRET=yyyyy
ANGEL_REDIRECT_URL=http://localhost:4000/api/broker/callback/angelone

